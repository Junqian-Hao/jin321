<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jin321.wx.dao.ProductDetailMapper">
    <select id="selectSimpleTogether" resultType="java.util.Map">
        SELECT *
        FROM product , productpics
        WHERE  product.is_delete=0
             AND productpics.is_deleted=0
            AND productpics.pid=product.pid AND productpics.is_headpic=1;
    </select>
</mapper>